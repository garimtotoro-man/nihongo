# 쇼츠말고 니혼고 환경변수 (이름만 적는다. 값은 .env.local 과 개인 Vercel 대시보드에만)
# 3차(계정·동기화) 전까지는 아무것도 필요 없다.

# --- 3차: 개인 Supabase (garimtotoro@gmail.com org) ---
# NEXT_PUBLIC_SUPABASE_URL=
# NEXT_PUBLIC_SUPABASE_ANON_KEY=

# Supabase CLI 를 쓸 때. `npx supabase login` 대신 이 값으로 인증해 전역 토큰 파일을 만들지 않는다.
# SUPABASE_ACCESS_TOKEN=
